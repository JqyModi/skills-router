{
  "name": "skills-router-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace=@skills-router/core"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
